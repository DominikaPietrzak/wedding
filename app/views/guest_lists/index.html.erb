<h2>List of all guests </h2>
<table class="table">
  <thead>
    <tr>
      <th> Imię </th>
      <th> Nazwisko </th>
      <th> </th>
    </tr>
  </thead>
  <tbody>
    <% @guests.each do |guest| %>
      <tr>
        <td><%= guest.name %> </td>
        <td><%= guest.surname %> </td>
        <td><%= link_to 'Edit', edit_guest_registration_path(guest)%></td>
      </tr>
    <% end %>
    <tr>
      <td>Suma gości </td>
      <td><%= @guests.count %> </td>
    </tr>
  </tbody>
</table>
