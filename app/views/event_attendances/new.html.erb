
<table class="table">
  <thead>

      <%= form_for @event_attendance do |f| %>


        <tr>
          <th scope="col">Imię </th>
          <th scope="col">Nazwisko </th>
          <% @events.each do |event| %>
            <th scope="col"> <%=  event.name %>  </th>
          <% end %>
        </tr>


  </thead>
<tbody>
  <tr>
    <td>Joanna</td>
    <td>Nowak</td>
    <% @events.each do |event| %>
     <td> <%= f.select :status,  EventAttendance.statuses.keys.map {|w| [w.humanize, w]} %> </td>
     <% end %>
    <% end %>
  </tr>
</tbody>
</table>
